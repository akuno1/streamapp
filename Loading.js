const Loading = (props) => {

    return (
        
            <div className="text-center pagination-centered centeringVertical">
                <h1> Loading your stuff :) </h1>
                <div className = "loader centering centeringVertical"> stuff </div> 
            </div>
       
    );
}

